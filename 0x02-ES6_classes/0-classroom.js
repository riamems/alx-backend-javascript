export default class ClassRoom {
  // Constructor for initializing the ClassRoom instance
  constructor(maxStudentsSize) {
    // Assign the maxStudentsSize parameter to the _maxStudentsSize attribute
    this._maxStudentsSize = maxStudentsSize;
  }
}
